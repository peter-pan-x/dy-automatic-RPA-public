# 抖音RPA自动化 - 使用指南

**版本**: V4.0  
**更新日期**: 2025-11-17  
**测试状态**: ✅ 100%成功

---

## 快速开始

### 1. 环境准备
```bash
# 确保安装：
- Python 3.8+
- Node.js (用于Appium)
- ADB工具
- Appium Server

# 安装依赖
pip install -r requirements.txt
npm install -g appium
appium driver install uiautomator2
```

### 2. 连接设备
```bash
# 连接手机并开启USB调试
adb devices  # 确认设备连接
```

### 3. 启动运行
```bash
# 方式1：一键启动（推荐）
一键启动环境.bat

# 方式2：手动启动
# 终端1：启动Appium
appium

# 终端2：运行程序
python src/main.py --auto-accept
```

---

## 核心功能

### ✅ 已验证功能（100%成功）

| 功能 | 成功率 | 说明 |
|------|--------|------|
| **点赞** | 100% | 自动点赞视频 |
| **评论** | 100% | 自动发送评论 |
| **收藏** | 100% | 自动收藏视频 |
| **关注** | 100% | 自动关注作者 |

### 运行模式

**1. 混合会话模式（推荐）**
- 随机切换"无聊刷"和"精确打击"
- 模拟真实用户行为
- 降低检测风险

**2. 概率执行**
- 每个操作按配置概率执行
- 默认概率可在配置文件调整

---

## 配置说明

### 主配置文件: `config/settings.py`

```python
# 会话视频数量
BORING_SESSION_VIDEOS = 15  # 无聊刷模式
PRECISE_SESSION_VIDEOS = 5   # 精确打击模式

# 交互概率
INTERACTION_PROBABILITIES = {
    "like": 0.6,      # 点赞概率60%
    "comment": 0.3,   # 评论概率30%
    "favorite": 0.2,  # 收藏概率20%
    "follow": 0.15    # 关注概率15%
}
```

### 运行时配置: `config/runtime_config.json`
支持热重载，无需重启程序即可生效。

### 评论内容: `config/comments.txt`
一行一个评论，程序会随机选择。

---

## 选择器配置（已优化）

### 关键成功选择器

**点赞**:
```python
By.ID: "com.ss.android.ugc.aweme:id/f_u"
By.XPATH: "//android.widget.ImageView[@content-desc and contains(@content-desc, '赞')]"
```

**评论**（6步流程）:
1. 点击评论按钮
2. 等待页面加载（3秒）
3. 点击输入框激活
4. 等待键盘弹出（2秒）
5. 重新查找输入框并输入
6. 点击发送，返回

**收藏**:
```python
By.XPATH: "//android.widget.LinearLayout[@content-desc and contains(@content-desc, '收藏')]"
```

**关注**:
```python
By.XPATH: "//android.widget.TextView[@text='关注']"
```

详细配置见 `config/selectors.py`

---

## 日志系统

### 日志文件
- `logs/douyin_rpa_YYYYMMDD.log` - 主日志
- `logs/error.log` - 错误日志
- `logs/douyin_stats.db` - 统计数据库

### 查看日志
```bash
# Windows
notepad logs\douyin_rpa_20251117.log

# 或使用彩色控制台输出
```

---

## 常见问题

### Q1: 找不到元素
**原因**: 抖音版本更新，选择器失效  
**解决**: 
1. 检查是否在视频播放页面
2. 更新 `config/selectors.py` 中的选择器
3. 查看日志确认实际错误

### Q2: 设备unauthorized
**解决**:
```bash
# 拔掉USB重新插
# 手机授权时勾选"始终允许"
adb devices  # 验证状态为device
```

### Q3: Appium连接失败
**解决**:
```bash
# 检查Appium是否运行
curl http://127.0.0.1:4723/status

# 重启Appium
taskkill /F /IM node.exe
appium
```

### Q4: 评论发送失败
**原因**: 元素过期或页面未加载完成  
**解决**: 已优化，采用"重新查找"策略，成功率100%

---

## 高级功能

### 智能元素检测器
`src/element_detector.py` 提供：
- 自动记录选择器成功率
- 动态调整选择器顺序
- 智能缓存提升性能

### 反检测机制
- 随机延迟
- 人性化滑动轨迹
- 行为模式模拟
- 时间窗口控制

### 数据统计
- SQLite数据库存储
- 操作记录导出
- 成功率统计

---

## 项目结构

```
douyin-autoclient/
├── src/                    # 源代码
│   ├── main.py            # 主程序入口
│   ├── interactions.py    # 交互逻辑（已优化）
│   ├── element_detector.py # 智能检测器
│   └── ...
├── config/                # 配置文件
│   ├── settings.py        # 主配置
│   ├── selectors.py       # 选择器（已优化）
│   └── comments.txt       # 评论内容
├── logs/                  # 日志目录
├── 一键启动环境.bat        # 启动脚本
├── README.md              # 项目说明
└── requirements.txt       # Python依赖
```

---

## 测试验证

**最新测试结果**（2025-11-17）:
```
浏览视频: 5个
总操作: 7次
成功操作: 7次
成功率: 100%
```

**各功能表现**:
- 点赞: 1/1 成功 (100%)
- 评论: 3/3 成功 (100%)
- 收藏: 1/1 成功 (100%)
- 关注: 2/2 成功 (100%)

---

## 更新记录

详见 `update_records.txt`

---

## 注意事项

1. **页面状态**: 确保在视频播放页面，不在评论页面
2. **设备授权**: USB调试需授权，建议勾选"始终允许"
3. **网络环境**: 操作需要稳定网络
4. **使用频率**: 建议控制使用频率，避免账号风险
5. **评论内容**: 使用正面友善的评论内容

---

## 许可与免责

仅供学习研究使用，请遵守相关法律法规和平台规则。

